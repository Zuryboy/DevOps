machine:
  node:
     version: 8.10
  services:
    - docker

dependencies:
  override:
   - docker build -t mzpl .

test:
  override:
    - docker run mzpl npm test

deployment:
  qa:
   branch: /feature_.*/
   commands:
      - sh deployments/qa.sh